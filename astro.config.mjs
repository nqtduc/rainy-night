// @ts-check
import { defineConfig } from 'astro/config';

// When you deploy, change `site` to your real domain (e.g. https://ducblog.com).
export default defineConfig({
  site: 'https://example.com',
});
